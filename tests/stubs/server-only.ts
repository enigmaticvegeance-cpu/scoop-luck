// Empty stub for the `server-only` package. Vitest aliases this module
// to here so that pure-unit tests can import server-only modules
// (lib/razorpay.ts, lib/stripe.ts, etc.) without Next.js's runtime
// guard throwing.
export {};
